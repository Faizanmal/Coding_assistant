name: update-fetch-api
description: Replace all usage of fetch() with axios() in code files
scope: src/test/sample/*.ts
prompt: |
  Replace all uses of the `{{old}}` function with the `{{new}}` function in the following TypeScript code.
  Preserve functionality and imports.

output: rewrite
